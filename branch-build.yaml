pr:
  branches:
    exclude:
      - main
  paths:
    include:
      - /*

pool:
  name: 'selfhosted'


jobs:
- job:
  displayName: 'Subscription vending branch build'
  steps: 
    - script: |
        az bicep build -f ./main.bicep
      displayName: 'lint'
