pr:
  branches:
    exclude:
      - main
  paths:
    include:
      - /*

pool:
  name: 'selfhosted'


jobs:
- job:
  displayName: 'branch build'
  steps: 
    - script: |
        az bicep lint -f ./main.bicepparam
      displayName: 'lint'
