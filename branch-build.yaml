pr:
  branches:
    include:
      - main
  paths:
    include:
      - /*

pool:
  name: 'selfhosted'


jobs:
- job:
  displayName: 'Subscription Vending Branch Build'
  steps: 
    - script: |
        az bicep build -f ./main.bicep
      displayName: 'lint'
